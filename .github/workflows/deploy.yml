name: Deploy to GitHub Pages

# 触发条件：推送到main分支或手动触发
on:
  push:
    branches: [ main ]
  # 允许手动触发
  workflow_dispatch:

# 设置权限
permissions:
  contents: read
  pages: write
  id-token: write

# 确保只有一个部署任务同时运行
concurrency:
  group: "pages"
  cancel-in-progress: false

# 全局环境变量 - 强制使用 Node.js 24
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # 构建任务
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Prepare for static export
        run: |
          # 删除本地环境文件，避免覆盖 GitHub Actions 的环境变量
          rm -f .env.local
          
          # 创建静态导出用的空 actions 实现
          echo "准备静态导出：创建空 actions 实现..."
          
          # 创建空实现文件
          cat > src/actions/index.ts << 'ENDOFFILE'
          // 静态导出模式 - 空实现
          // 此文件在 GitHub Pages 构建时自动生成
          
          // ==================== 类型定义 ====================
          
          export interface BlogPost {
            id: string;
            title: string;
            slug: string;
            date: string;
            category: string;
            tags: string[];
            excerpt: string;
            content: string;
            coverImage?: string;
            readingTime?: number;
            wordCount?: number;
            updatedAt?: string;
            hidden?: boolean;
            pinned?: boolean;
            pinnedAt?: string;
            filePath: string;
          }
          
          export interface BlogPostData {
            title: string;
            content: string;
            date?: string;
            category: string;
            tags?: string[];
            excerpt?: string;
            coverImage?: string;
            slug?: string;
            hidden?: boolean;
          }
          
          export interface ActionResult<T = any> {
            success: boolean;
            message: string;
            data?: T;
            filePath?: string;
          }
          
          export interface MomentData {
            time: string;
            content: string;
            tags?: string[];
            images?: string[];
            pinned?: boolean;
            hidden?: boolean;
          }
          
          export interface Moment {
            id: string;
            time: string;
            content: string;
            tags: string[];
            images: string[];
            pinned: boolean;
            hidden: boolean;
          }
          
          export enum ImageSource {
            Local = 'local',
            Remote = 'remote'
          }
          
          export interface GalleryImage {
            id: string;
            src: string;
            fallbackSrc?: string;
            thumbnail?: string;
            alt: string;
            title?: string;
            category: string;
            subCategory?: string;
            source: ImageSource;
            createdAt: string;
            updatedAt?: string;
            width?: number;
            height?: number;
            size?: number;
          }
          
          export interface UploadResult {
            success: boolean;
            message: string;
            image?: GalleryImage;
          }
          
          export interface UploadMultipleResult {
            success: boolean;
            message: string;
            images: GalleryImage[];
            failed: string[];
          }
          
          export interface GalleryStats {
            totalImages: number;
            totalSize: number;
            directories: number;
          }
          
          export interface DirectoryTree {
            name: string;
            path: string;
            type: 'directory' | 'file';
            size?: number;
            children?: DirectoryTree[];
            imageCount?: number;
          }
          
          export interface TodoItem {
            id: string;
            content: string;
            completed: boolean;
            priority: 'high' | 'medium' | 'low';
            dueDate?: string;
            createdAt: string;
            updatedAt?: string;
          }
          
          export interface TodoConfig {
            title: string;
            items: TodoItem[];
            showStats: boolean;
          }
          
          export interface TodoFormData {
            content: string;
            priority?: 'high' | 'medium' | 'low';
            dueDate?: string;
          }
          
          export interface TodoActionResult<T = any> {
            success: boolean;
            message: string;
            data?: T;
          }
          
          export interface SystemSettings {
            siteName: string;
            siteDescription: string;
            siteUrl: string;
            author: string;
            email: string;
            metaTitle: string;
            metaDescription: string;
            keywords: string;
            ogImage: string;
            github: string;
            twitter: string;
            weibo: string;
            bilibili: string;
            socialEmail: string;
            enablePwa: boolean;
            enableAnalytics: boolean;
            enableComments: boolean;
            maintenanceMode: boolean;
          }
          
          export interface BackupResult {
            success: boolean;
            message: string;
            backupPath?: string;
            commitHash?: string;
            filesCount?: number;
            timestamp?: string;
          }
          
          export interface BackupHistory {
            commitHash: string;
            message: string;
            timestamp: string;
            filesCount: number;
          }
          
          export interface PushConfig {
            remoteUrl: string;
            branch?: string;
            token?: string;
          }
          
          export interface GitPushResult {
            success: boolean;
            message: string;
            commitHash?: string;
            pushedFiles?: string[];
          }
          
          export interface GitStatus {
            isRepo: boolean;
            hasRemote: boolean;
            currentBranch: string;
            hasUncommittedChanges: boolean;
            uncommittedFiles: string[];
            trackingBranch?: string;
            hasPushable: boolean;
            aheadCount: number;
            behindCount: number;
          }
          
          // ==================== 函数实现 ====================
          
          // Blog Actions
          export async function getBlogList() { return [] as BlogPost[]; }
          export async function getBlogDetail(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function createBlog(data: BlogPostData) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateBlog(id: string, data: Partial<BlogPostData>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function deleteBlog(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function batchDeleteBlogs(ids: string[]) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function batchUpdateBlogCategory(ids: string[], category: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function getBlogCategories() { return [] as string[]; }
          export async function saveBlogMarkdown(slug: string, content: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function toggleBlogHidden(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function batchToggleBlogHidden(ids: string[], hidden: boolean) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          
          // Moment Actions
          export async function getMomentList() { return { success: false, message: '静态导出模式不支持此功能', data: [] as Moment[] }; }
          export async function getMomentDetail(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function createMoment(data: MomentData) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateMoment(id: string, data: Partial<MomentData>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function deleteMoment(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function batchDeleteMoments(ids: string[]) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function toggleMomentPinned(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function batchToggleMomentPinned(ids: string[], pinned: boolean) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function generateNewMomentId() { return 'moment-' + Date.now(); }
          export async function getMomentTags() { return [] as string[]; }
          export async function toggleMomentHidden(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function batchToggleMomentHidden(ids: string[], hidden: boolean) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          
          // Gallery Actions
          export async function getLocalGalleryImages(path?: string) { return [] as GalleryImage[]; }
          export async function getLocalGalleryDirectories() { return [] as string[]; }
          export async function getLocalGalleryDirectoryTree() { return [] as DirectoryTree[]; }
          export async function getLocalGallerySubDirectories(path?: string) { return [] as string[]; }
          export async function deleteLocalImage(path: string): Promise<UploadResult> { return { success: false, message: '静态导出模式不支持此功能' }; }
          export async function uploadLocalImage(formData?: FormData, path?: string): Promise<UploadResult> { return { success: false, message: '静态导出模式不支持此功能' }; }
          export async function uploadLocalImages(formDataList?: FormData[], path?: string) { return { success: false, message: '静态导出模式不支持此功能', images: [] as GalleryImage[], failed: [] as string[] }; }
          export async function getLocalGalleryStats() { return { totalImages: 0, totalSize: 0, directories: 0 }; }
          
          // Todo Actions
          export async function getTodoConfig() { return { success: true, message: '获取成功', data: { title: '待办事项', items: [] as TodoItem[], showStats: true } }; }
          export async function getTodoList() { return { success: true, message: '获取成功', data: [] as TodoItem[] }; }
          export async function getTodoItem(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function createTodoItem(data: TodoFormData) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateTodoItem(id: string, data: Partial<TodoFormData>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function deleteTodoItem(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function toggleTodoComplete(id: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function batchDeleteTodoItems(ids: string[]) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateTodoConfigSettings(settings: Partial<Pick<TodoConfig, 'title' | 'showStats'>>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          
          // Settings Actions
          export async function getSettings() { return { siteName: 'Blog', siteDescription: '', siteUrl: '', author: '', email: '', metaTitle: '', metaDescription: '', keywords: '', ogImage: '', github: '', twitter: '', weibo: '', bilibili: '', socialEmail: '', enablePwa: true, enableAnalytics: false, enableComments: true, maintenanceMode: false }; }
          export async function saveSettings(settings: Partial<SystemSettings>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function resetSettings() { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateGeneralSettings(general: Partial<SystemSettings>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateSeoSettings(seo: Partial<SystemSettings>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateSocialSettings(social: Partial<SystemSettings>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function updateAdvancedSettings(advanced: Partial<SystemSettings>) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          
          // Backup Actions
          export async function changeRestorePassword() { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function verifyRestorePassword() { return false; }
          export async function getBackupPath() { return ''; }
          export async function backupDirExists() { return false; }
          export async function initBackupRepo() { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function performBackup() { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function getBackupHistory(limit?: number) { return { success: false, message: '静态导出模式不支持此功能', history: [] as BackupHistory[] }; }
          export async function restoreBackup(commitHash?: string, password?: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function getBackupStatus() { return { success: false, message: '静态导出模式不支持此功能', totalCommits: 0, lastBackup: '', trackedFiles: 0, hasRemote: false, remoteUrl: '' }; }
          export async function configureRemote(config?: PushConfig) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function pushToRemote() { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function getRemoteInfo() { return { success: false, message: '静态导出模式不支持此功能', remoteUrl: '', branch: 'main', ahead: 0 }; }
          export async function testRemoteConnection(config?: PushConfig) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          
          // GitHub Actions
          export async function initGitRepo() { return { success: false, message: '静态导出模式不支持此功能', isRepo: false, hasRemote: false }; }
          export async function getGitStatus() { return { isRepo: false, hasRemote: false, currentBranch: '', hasUncommittedChanges: false, uncommittedFiles: [], hasPushable: false, aheadCount: 0, behindCount: 0 }; }
          export async function hasUncommittedChanges() { return false; }
          export async function isAheadOfRemote() { return false; }
          export async function getUncommittedFiles() { return { modified: [] as string[], added: [] as string[], deleted: [] as string[], renamed: [] as string[] }; }
          export async function pushToGitHub(message?: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function buildAndPush(buildMessage?: string, pushMessage?: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function addRemote(remoteName?: string, remoteUrl?: string) { return { success: false, message: '静态导出模式不支持此功能', data: null }; }
          export async function getRemoteList() { return [] as Array<{ name: string; url: string }>; }
          export async function getCommitHistory(count?: number) { return [] as Array<{ hash: string; message: string; date: string; author: string }>; }
          ENDOFFILE
          
          echo "空 actions 实现创建完成"

      - name: Build project
        env:
          # 设置GitHub Actions环境变量 - 使用 env 块确保变量被正确传递
          NODE_ENV: production
          NEXT_PUBLIC_GITHUB_REPO_NAME: ${{ github.event.repository.name }}
          NEXT_PRIVATE_STATIC_EXPORT: "true"
          STATIC_EXPORT: "true"
          CUSTOM_DOMAIN: "true"
          NEXT_PUBLIC_SITE_URL: "https://blog.xinchengp.cn"
          NEXT_PUBLIC_BASE_PATH: ""
        run: |
          # 输出构建环境信息
          echo "构建环境信息："
          echo "NODE_ENV: $NODE_ENV"
          echo "NEXT_PUBLIC_GITHUB_REPO_NAME: $NEXT_PUBLIC_GITHUB_REPO_NAME"
          echo "NEXT_PUBLIC_BASE_PATH: $NEXT_PUBLIC_BASE_PATH"
          echo "NEXT_PUBLIC_SITE_URL: $NEXT_PUBLIC_SITE_URL"
          echo "CUSTOM_DOMAIN: $CUSTOM_DOMAIN"
          echo "NEXT_PRIVATE_STATIC_EXPORT: $NEXT_PRIVATE_STATIC_EXPORT"
          echo "STATIC_EXPORT: $STATIC_EXPORT"
          # 执行构建
          npm run sync-theme && npx next build

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          static_site_generator: next

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  # 部署任务
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
